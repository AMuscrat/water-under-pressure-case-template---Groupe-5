# Session log — e262972

## Entry 1 — 2026-09-16
User requested enforcement of the login session in app.js and removal of dead app-core.js.

## Result
Added the early session redirect and sign-out cleanup to app.js, removed the dynamic app-core.js load, and deleted app-core.js.

## Follow-up

Restored app-core.js and its dynamic load because it is a real runtime dependency containing the crop scenarios, region state, dashboard rendering, slider listeners, and UI stylesheet injection. It is not dead code: app.js loads it dynamically during dashboard initialization.
