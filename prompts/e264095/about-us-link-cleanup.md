# About Us link cleanup — e264095

## Request
Remove the hyperlinks from the About Us section and style the “Back to top” control so it matches the AquaCrop design. Create a new pull request and keep the work attributed to student ID e264095.

## Implementation
The About Us navigation links are removed from the visible layout, and the Back to top control uses the existing teal palette, border radius, border, shadow, hover and focus treatments.
