# Simple user database — e264095

## Request
Create a simple database for users and passwords using a text or JSON file.

## Result
Added `data/users.json` for prototype/demo credentials. The file is explicitly marked as prototype-only and should not be used for real user passwords in a public static repository.
